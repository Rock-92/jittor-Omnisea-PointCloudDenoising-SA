name: jittor
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.9
  - gcc=10
  - gxx=10
  - libgomp
  - pip
  - pip:
      # requirements.txt includes cupy-cuda11x for Jittor CUDA scalar ops.
      - -r requirements.txt
