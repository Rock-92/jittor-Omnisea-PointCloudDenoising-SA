mode: train
debug: false

# The denoising VM and all per-layer cross-attention modules train from scratch.
# Only the Shape Processor is initialized from shape_pretrained_ckpt.
load_ckpt: null

components:
  data: shape_context_vm
  transform: shape_context_vm_laplace
  system: shape_context_vm
  model: shape_context_vm
  train: shape_context_vm
