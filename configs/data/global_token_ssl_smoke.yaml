loader: &loader geometry_ssl

train_dataset:
  shuffle: True
  batch_size: 2
  num_workers: 0
  datapath:
    input_dataset_dir: ./geometry_ssl_cache_smoke
    use_prob: False
    loader: *loader
    data_name: null
    ignore_check: True
    data_path:
      ssl: [
        [./geometry_ssl_cache_smoke/train.txt, 1.0],
      ]

validate_dataset: null
predict_dataset: null
