data_name: &data_name null
loader: &loader geometry_ssl

train_dataset:
  shuffle: True
  batch_size: 64
  num_workers: 8
  datapath:
    input_dataset_dir: ./geometry_ssl_cache
    use_prob: False
    loader: *loader
    data_name: *data_name
    ignore_check: True
    data_path:
      ssl: [
        [./geometry_ssl_cache/train.txt, 1.0],
      ]

validate_dataset: null
predict_dataset: null
